#include <stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d", arr[i]);
    }
    int index;
    scanf("%d", &index);
    for (int i = index; i < count; i++)
    {
        /* code */
    }
    
    
    return 0;
}